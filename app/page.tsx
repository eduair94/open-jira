import { Typography } from '@mui/material';
import { NextPage } from 'next';

const Home: NextPage = () => (
  <Typography variant="h1" color="primary">
    Hello World
  </Typography>
);

export default Home;
